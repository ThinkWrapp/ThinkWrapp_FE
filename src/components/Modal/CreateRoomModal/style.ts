import styled from 'styled-components';

export const CreateRoomModalHeader = styled.div`
    position: relative;
    display: flex;
    padding: 0rem 1.5rem 0.8rem;
`;

export const CreateRoomModalForm = styled.form`
    padding: 2rem 1.5rem;
`;

export const CheckPasswordWrapper = styled.div`
    display: flex;
    align-items: center;
    margin: 3.5rem 0;
    gap: 1rem;
`;

export const CreateRoomButtonGroup = styled.div`
    display: flex;
    justify-content: space-between;
    margin-top: 2.5rem;
    gap: 1rem;
`;
