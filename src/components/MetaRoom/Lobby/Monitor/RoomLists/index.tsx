import RoomList from '../RoomList';
import { Lists } from './style';

const RoomLists = () => {
    return (
        <Lists>
            <RoomList />
        </Lists>
    );
};

export default RoomLists;
